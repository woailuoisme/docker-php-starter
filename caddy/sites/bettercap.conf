# 如果有域名，替换为域名：example.com
# 如果仅本地测试，使用 :80
#:80 {
#    # 反向代理到 bettercap UI
#    # 注意：bettercap 默认 UI 端口通常是 8080，
#    # 但在 compose 命令中我们改成了 8081 以防冲突
#    reverse_proxy 127.0.0.1:8081
#    # 可选：限制访问 IP 以增强安全性
#    # remote_ip 192.168.1.0/24
#    log {
#        output file /var/log/caddy/access.log
#    }
#}